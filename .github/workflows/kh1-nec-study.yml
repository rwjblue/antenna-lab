name: KH1 NEC-2 study

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  study:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - run: echo 'Workflow temporarily staged for source transfer.'
