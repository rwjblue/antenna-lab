# ChatGPT bundle v0.1: historical provenance only

This directory records metadata for the earlier reproducibility bundle. The
byte-for-byte archive is `../source-bundles/58ft_doublet_reproducibility_bundle_v0.1.zip`.

v0.1 froze construction context, measured impedance, KXAT2 observations, and
the analytical radiation-pattern model before the joint optimizer existed. Its
planned YAML optimizer config and any provisional absolute-efficiency discussion
are historical, not current model state.

The pattern source/report in v0.1 is byte-identical to the copy carried forward
inside v1.0. Important pattern artifacts are exposed under
`reports/radiation-pattern-v0.1/`; the archive remains the authority for the
original packaging.
